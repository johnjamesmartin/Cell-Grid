/* Conveniently config the grid you wish to create by changing
   the following properties:
 ************************************************************/
const gridConfig = {
  size: 12, // number is squared to make up number of cells (144 in this case)
  scale: 0.8, // 0 - 1
  width: 750,
  border: {
    color: '#000',
    cellBorder: '#CCC',
    style: 'solid',
    width: 5,
    rounded: true
  }
};

export { gridConfig };
